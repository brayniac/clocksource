language: rust
rust:
- stable
- beta
- nightly
os:
- linux
- osx
after_success: sh scripts/travis-doc-upload.sh
matrix:
  include:
    - os: linux
      rust: nightly
      script: cargo bench --features asm
    - os: osx
      rust: nightly
      script: cargo bench --features asm